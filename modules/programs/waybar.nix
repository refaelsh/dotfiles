{ ... }:
{
  # UNUSED (xmonad + xmobar active)
  #
  # This file is kept as a stub so that import-tree continues to work without error.
  # The waybar configuration is not used in the current xmonad + xmobar setup.
  #
  # Safe to `git rm` this file in a follow-up commit once confirmed unnecessary.
  flake.nixosModules.waybar = { ... }: { };
}
